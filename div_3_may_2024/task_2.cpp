#include <bits/stdc++.h>
#define all(x) x.begin(), x.end()

using namespace std;

struct info{
    int ones, zeros;
};

void solve(){
    int n, m;
    cin >> n >> m;
    string a, b;
    cin >> a >> b;
    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--){
        solve();
    }
}