#include <bits/stdc++.h>
    
using namespace std;
using graph = vector<vector<int>>;
    
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    vector<int> a = {1, 2, 3};
    vector<int> b = a;
    for (int i = 0; i < b.size(); i++){
        cout << b[i] << '\n';
    }
}