#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie();
    int n, m;
    cin >> n >> m;
    if (n == m){
        cout << 2 << '\n';
    } else {
        cout << 1 << '\n';
    }
}