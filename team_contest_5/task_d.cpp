#include <bits/stdc++.h>

using namespace std;

struct left{
    int petrol, money;
};
struct station{
    int coord, price;
};

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, b, c;
    cin >> n >> b >> c;

}